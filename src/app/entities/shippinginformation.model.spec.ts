import { ShippingInformation } from './shippinginformation.model';

describe('ShippingInformation.Model', () => {
  it('should create an instance', () => {
    expect(new ShippingInformation()).toBeTruthy();
  });
});
